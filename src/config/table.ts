export const tableConfig = {
    pagination: {
        pageSize: 20,
        pageIndex: 0
    }
}

export const trimOnlyColumns = [
    'gauge',
    'assigned',
    'release_to_production',
    'cutting_complete'
]
